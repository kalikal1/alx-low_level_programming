#include <stdio.h>

/**
* main - Prints the alphabetic
*
* Return: Always (Succes)
*/
int main(void)
{
    char a, b;
    
    for (a= 'a'; c<= 'z'; a++)
      putchar(a);
    
    for (b='A'; c <= 'Z'; b++)
     putchar(b);
    
    putchar('\n');
    return (0);
}
